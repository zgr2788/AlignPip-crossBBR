sampleTable_prefix: sampleTable_final    #Write only the name of the file, NOT the extension. Ex. "sampleTable.csv -> sampleTable"

STARindexFolder: /cta/users/oybeker/GRCh38_index/index    #Write the ABSOLUTE PATH of the index generated by STAR. Will be used in the alignment step
